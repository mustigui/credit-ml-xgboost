FROM python:3.12-slim

RUN pip install --no-cache-dir "mlflow>=3.1.0,<4"

EXPOSE 5000
VOLUME ["/mlflow-data"]

ENTRYPOINT ["mlflow", "server", "--host", "0.0.0.0", "--port", "5000", "--backend-store-uri", "sqlite:////mlflow-data/mlflow.db", "--default-artifact-root", "/mlflow-data/artifacts"]
